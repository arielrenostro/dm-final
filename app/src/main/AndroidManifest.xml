<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.furb.carmonitorfinal">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />
    <uses-permission android:name="com.google.android.libraries.car.app.NAVIGATION_TEMPLATES"/>
    <uses-permission android:name="com.google.android.libraries.car.app.ACCESS_SURFACE"/>


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <receiver android:name=".services.MessageReadReceiver">
            <intent-filter>
                <action android:name="com.furb.carmonitorfinal.ACTION_MESSAGE_READ" />
            </intent-filter>
        </receiver>

        <receiver android:name=".services.MessageReplyReceiver">
            <intent-filter>
                <action android:name="com.furb.carmonitorfinal.ACTION_MESSAGE_REPLY" />
                <data
                    android:host="reply/message"
                    android:scheme="carservice" />
            </intent-filter>
        </receiver>

        <service
            android:name=".services.FuelNotificationService"
            android:enabled="true"
            android:exported="false" />
        <service android:name=".services.OBD2Service" />

        <service
            android:name=".services.CarService"
            android:exported="false">

            <intent-filter>
                <action android:name="com.google.android.car.action.CAR_APP" android:category="com.google.android.car.category.NAVIGATION"/>
            </intent-filter>
        </service>

        <meta-data
            android:name="com.google.android.gms.car.application"
            android:resource="@xml/automotive_app_desc" />

        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>